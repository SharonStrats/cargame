{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "prebuild": "npm run build --prefix ../packages/shared",
    "dev": "vite",
    "dev:raw": "vite",
    "dev:backend": "npm --prefix ../backend run dev",
    "dev:all": "node scripts/dev-all.mjs",
    "predev": "npm run build --prefix ../packages/shared",
    "watch": "tsc -w --noEmit",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.0.4"
  },
  "dependencies": {
    "@cargame/shared": "file:../packages/shared",
    "@types/three": "^0.184.0",
    "cannon-es": "^0.20.0",
    "lit": "^3.3.3",
    "socket.io-client": "^4.8.3",
    "three": "^0.183.2"
  }
}
